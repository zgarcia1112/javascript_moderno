///////////////////////////////////////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////For in///////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////////////////////
// For in intera sobre objetos -- como tal for in trae lo que es la propiedad del objeto. para traer el valor de la propiedad se puede hacer lo siguiente

const automovil = {
  nombre: "camaro",
  year: 2020,
  motor: "6.0",
};

for (let propiedad in automovil) {
  //   console.log(`${automovil[propiedad]}`);
}

for (let [llave, valor] of Object.entries(automovil)) {
  console.log(llave);
  console.log(valor);
}
