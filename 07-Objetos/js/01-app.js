const nombre = "Monitor 20 pulgadas";
const precio = 300;
const disponible = true;

//un objeto agrupa todo en una sola variable

// object literal : si el la variable esta declarada asi const producto = {} es un buen idicativo de es un objeto
//dentro del objeto tenemos la propiedad o llave y despues el valor de la llave. ejemplo  nombre: "Monitor 20 pulgadas", nombre = llave, "monitor 20 pulgadas" = valor de la llave

const producto = {
  nombre: "monitor 20 pulgadas",
  precio: 300,
  disponible: true,
};

console.log(producto);
