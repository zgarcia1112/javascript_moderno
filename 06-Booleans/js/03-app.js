const autenticado = true;

// if (autenticado) {
//   //el true se da como implicito cuando hay un if el implicito es el true ejemplo   if (autenticado === true)   seria lo mismo que if (autenticado)
//   console.log("si puedes ver netflix");
// } else {
//   console.log("no puedes ver netflix");
// }

// operador ternario, lo de arriba es lo mismo que abajo.
//solo sirve cuando tenemos 1 linea de codigo dentro del if y el else

console.log(autenticado ? "Si estas autenticado" : "No esta autenticado");
